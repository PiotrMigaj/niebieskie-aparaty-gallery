// Same as event/mod.rs — this is the entry point for the `gallery` module.
// Declaring sub-modules here makes `gallery::model` and `gallery::repository`
// accessible from the rest of the crate.

pub mod model;
pub mod repository;
